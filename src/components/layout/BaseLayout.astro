<style is:global>
    .layout-page {
        min-height: 100vh;
        overflow-x: clip;
    }

    .layout-section {
        padding-block: clamp(2rem, 5vw, 5rem);
    }

    .layout-container {
        width: min(calc(100% - clamp(1rem, 4vw, 4rem)), 1280px);
        margin-inline: auto;
    }

    .layout-container-wide {
        width: min(calc(100% - clamp(1rem, 3vw, 3rem)), 1536px);
        margin-inline: auto;
    }

    .layout-container-narrow {
        width: min(calc(100% - clamp(1rem, 5vw, 5rem)), 860px);
        margin-inline: auto;
    }

    .layout-full-bleed {
        width: 100%;
    }

    .layout-debug {
        border: 2px solid red;
        background:rgba(30, 80, 232, 0.43);
    }

    .layout-demo-block {
        min-height: 180px;
    }

</style>